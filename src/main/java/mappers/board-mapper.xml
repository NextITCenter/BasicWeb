<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="board.BoardMapper">
	<select id="selectBoards" resultType="board.BoardDTO">
		SELECT
			ID,
			TITLE,
			CONTENT,
			WRITER,
			HITS,
			REGISTER_DATE,
			MODIFIED_DATE
		FROM
			BOARD
	</select>
	<select id="selectBoard" parameterType="int" resultType="board.BoardDTO">
		SELECT
			ID,
			TITLE,
			CONTENT,
			WRITER,
			HITS,
			REGISTER_DATE,
			MODIFIED_DATE
		FROM
			BOARD
		WHERE
			ID = #{id}
	</select>
	<insert id="registerBoard" parameterType="board.BoardDTO">
		insert into
			board
			(title, content, writer)
		values
			(#{title}, #{content}, #{writer})
	</insert>
	<update id="modifyBoard" parameterType="board.BoardDTO">
		update
			board
		set
			title = #{title},
			content = #{content},
			writer = #{writer},
			modified_date = sysdate
		where
			id = #{id}
	</update>
	<delete id="removeBoard" parameterType="int">
		delete from
			board
		where
			id = #{id}
	</delete>
	
	<!-- 첨부파일 저장 -->
	<insert id="insertFile" parameterType="board.FileDTO">
		insert into
			attached_file
			(board_id, file_path, file_name, original_name, file_size)
		values
			(#{boardId}, #{filePath}, #{fileName}, #{originalName}, #{fileSize})
	</insert>
</mapper>











